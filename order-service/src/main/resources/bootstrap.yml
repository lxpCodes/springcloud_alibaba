spring:
  application:
    name: order-service #微服务id
  profiles:
    active: prd #指定环境名
  cloud:
    nacos:
      config:
        file-extension: yml
        server-addr: 192.168.3.67:8848
        username: nacos
        password: nacos
logging:
  level:
    root: debug